name: baseline_gold
defaults:
  lineage:
    enabled: true
    capture_contract_name: true
  quarantine:
    enabled: true
    include_error_reason: true
  metadata:
    approval_required: true
    approval_quarantine_ratio_threshold: 0.05
gold_defaults:
  quality:
    dataset_rules:
      - row_count_between:
          min: 1
