# LakeLogic Registry — Multi-Tenant Isolation Example
# Run with: LAKELOGIC_ENV=dev-acme lakelogic-driver --registry contracts/_registry.yaml --layers bronze,silver,gold

entries:
  - entity: events
    enabled: true
    contracts:
      bronze: bronze_events.yaml
      silver: silver_events.yaml
      gold:   gold_tenant_kpi.yaml
