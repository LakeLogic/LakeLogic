version: 1.0.0
info:
  title: Bronze Clicks
  version: 1.0.0
  target_layer: bronze
dataset: bronze_clicks
source:
  type: landing
  path: ../../data/clicks.csv
  load_mode: full
server:
  type: local
  format: parquet
  path: ../../output/bronze/clicks
  mode: ingest
  schema_evolution: strict
  allow_schema_drift: false
  cast_to_string: true
materialization:
  target_path: ../../output/bronze/clicks
  format: parquet
  strategy: append
