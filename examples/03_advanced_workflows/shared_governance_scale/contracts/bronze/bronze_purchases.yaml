version: 1.0.0
info:
  title: Bronze Purchases
  version: 1.0.0
  target_layer: bronze
dataset: bronze_purchases
source:
  type: landing
  path: ../../data/purchases.csv
  load_mode: full
server:
  type: local
  format: parquet
  path: ../../output/bronze/purchases
  mode: ingest
  schema_evolution: strict
  allow_schema_drift: false
  cast_to_string: true
materialization:
  target_path: ../../output/bronze/purchases
  format: parquet
  strategy: append
