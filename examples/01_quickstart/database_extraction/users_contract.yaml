version: 1.0.0
dataset: local_users
description: "User data extraction from local SQLite"

source:
  type: database
  connector: sqlite
  connection:
    database: example.db
  query: "SELECT * FROM users"

model:
  fields:
    - name: id
      type: integer
    - name: name
      type: string
    - name: email
      type: string

materialization:
  bronze:
    enabled: true
    path: ./data/bronze/users/
    format: delta
    mode: overwrite
