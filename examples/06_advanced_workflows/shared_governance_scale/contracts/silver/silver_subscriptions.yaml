version: 1.0.0
info:
  title: Silver Subscriptions
  target_layer: silver
dataset: silver_subscriptions
source:
  type: landing
  path: ../../data/subscriptions.csv
  load_mode: full
metadata:
  domain: growth
  system: app_events
model:
  fields:
  - name: subscription_id
    type: string
    required: true
  - name: user_id
    type: string
    required: true
  - name: plan
    type: string
  - name: status
    type: string
  - name: updated_at
    type: timestamp
materialization:
  target_path: ../../output/silver/subscriptions
  format: parquet
  strategy: append
transformations:
- deduplicate:
    'on':
    - subscription_id
    sort_by:
    - updated_at
    order: desc
  phase: pre
