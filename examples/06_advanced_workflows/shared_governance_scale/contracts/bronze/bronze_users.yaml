version: 1.0.0
info:
  title: Bronze Users
  version: 1.0.0
  target_layer: bronze
dataset: bronze_users
source:
  type: landing
  path: ../../data/users.csv
  load_mode: full
server:
  type: local
  format: parquet
  path: ../../output/bronze/users
  mode: ingest
  schema_evolution: strict
  allow_schema_drift: false
  cast_to_string: true
materialization:
  target_path: ../../output/bronze/users
  format: parquet
  strategy: append
