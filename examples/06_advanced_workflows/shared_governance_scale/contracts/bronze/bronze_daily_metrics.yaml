version: 1.0.0
info:
  title: Bronze Daily Metrics
  target_layer: bronze
dataset: bronze_daily_metrics
source:
  type: landing
  path: ../../data/daily_metrics.csv
  load_mode: full
server:
  type: local
  format: parquet
  path: ../../output/bronze/daily_metrics
  mode: ingest
  schema_evolution: strict
  allow_schema_drift: false
  cast_to_string: true
materialization:
  target_path: ../../output/bronze/daily_metrics
  format: parquet
  strategy: append
