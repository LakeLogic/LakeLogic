version: 1.0.0
info:
  title: Bronze Subscriptions
  target_layer: bronze
dataset: bronze_subscriptions
source:
  type: landing
  path: ../../data/subscriptions.csv
  load_mode: full
server:
  type: local
  format: parquet
  path: ../../output/bronze/subscriptions
  mode: ingest
  schema_evolution: strict
  allow_schema_drift: false
  cast_to_string: true
materialization:
  target_path: ../../output/bronze/subscriptions
  format: parquet
  strategy: append
