version: 1.0.0
info:
  title: Bronze Sessions
  target_layer: bronze
dataset: bronze_sessions
source:
  type: landing
  path: ../../data/sessions.csv
  load_mode: full
server:
  type: local
  format: parquet
  path: ../../output/bronze/sessions
  mode: ingest
  schema_evolution: strict
  allow_schema_drift: false
  cast_to_string: true
materialization:
  target_path: ../../output/bronze/sessions
  format: parquet
  strategy: append
