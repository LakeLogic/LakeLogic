version: 1.0.0
info:
  title: Bronze Device Events
  version: 1.0.0
  target_layer: bronze
dataset: bronze_device_events
source:
  type: landing
  path: ../../data/device_events.csv
  load_mode: full
server:
  type: local
  format: parquet
  path: ../../output/bronze/device_events
  mode: ingest
  schema_evolution: strict
  allow_schema_drift: false
  cast_to_string: true
materialization:
  target_path: ../../output/bronze/device_events
  format: parquet
  strategy: append
