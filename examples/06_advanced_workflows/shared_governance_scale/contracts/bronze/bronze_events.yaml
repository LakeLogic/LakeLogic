version: 1.0.0
info:
  title: Bronze Events
  target_layer: bronze
dataset: bronze_events
source:
  type: landing
  path: ../../data/events.csv
  load_mode: full
server:
  type: local
  format: parquet
  path: ../../output/bronze/events
  mode: ingest
  schema_evolution: strict
  allow_schema_drift: false
  cast_to_string: true
materialization:
  target_path: ../../output/bronze/events
  format: parquet
  strategy: append
