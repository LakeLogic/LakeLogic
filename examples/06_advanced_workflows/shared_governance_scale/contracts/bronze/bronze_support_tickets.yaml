version: 1.0.0
info:
  title: Bronze Support Tickets
  target_layer: bronze
dataset: bronze_support_tickets
source:
  type: landing
  path: ../../data/support_tickets.csv
  load_mode: full
server:
  type: local
  format: parquet
  path: ../../output/bronze/support_tickets
  mode: ingest
  schema_evolution: strict
  allow_schema_drift: false
  cast_to_string: true
materialization:
  target_path: ../../output/bronze/support_tickets
  format: parquet
  strategy: append
