version: 1.0.0
info:
  title: Bronze Feature Flags
  target_layer: bronze
dataset: bronze_feature_flags
source:
  type: landing
  path: ../../data/feature_flags.csv
  load_mode: full
server:
  type: local
  format: parquet
  path: ../../output/bronze/feature_flags
  mode: ingest
  schema_evolution: strict
  allow_schema_drift: false
  cast_to_string: true
materialization:
  target_path: ../../output/bronze/feature_flags
  format: parquet
  strategy: append
